# remap prefix from C-b to CapsLock
unbind C-b
set-option -g prefix F10
bind-key F10 last-window

# Use truecolors
set -g default-terminal "screen-256color"
set -ga terminal-overrides ",*256col*:Tc"

